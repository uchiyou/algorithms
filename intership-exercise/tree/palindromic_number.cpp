/*
所谓回文字符串，就是一个字符串，从左到右读和从右到左读是完全一样的，比如"aba"、"c"。
对于一个字符串，可以通过删除某些字符而变成回文字符串，如"cabebaf"，删除'c'、'e'、'f'后剩下子串"abba"就是回文字符串。
要求，给定任意一个字符串，字符串最大长度1000，计算出最长的回文字符串长度。如"cabebaf"的回文串包括"c"、"aba"、"abba"等，最长回文"abba"长度为4。
输入：字符串
输出：最大的回文字符串长度。
示例：输入: cabbeaf
输出: 4

*/


